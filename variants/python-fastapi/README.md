# Variant: Python + FastAPI

This is a minimal, working API-first starter:
- FastAPI app with `/health`
- pytest coverage for the endpoint
- ruff lint/format
- Makefile wired (setup/test/lint/format/run)

Quickstart:
```bash
make setup
make test
make lint
make run
```
